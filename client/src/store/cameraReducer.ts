//interface CamerasObjectArray {
//  camName: string,
//}

//interface GetCameras {
//  camArray: Array<
//}

/*
const initialState:

  export const cameraReducer = (state = defaultState, action) => {
    switch (action.type) {



      default:
        return state
    }
  }
  */

export default 1
