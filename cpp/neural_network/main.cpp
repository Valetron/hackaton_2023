//#include "NeuralNetwork.h"

//int main(int argc, char *argv[])
//{
//    NeuralNetwork nn(argv[1]);
//    Colors cl = Colors();

////    cv::Mat img = imread(argv[2], cv::IMREAD_COLOR);
//    auto cap = cv::VideoCapture(argv[2]);

//    cv::Mat frame;

//    while(cap.isOpened())
//    {
//        cap >> frame;
//        auto detections = nn.detect(frame);

//        nn.postProcess(frame, detections, cl);
//    }

////    auto detections = nn.detect(img);

////    nn.postProcess(img, detections, cl);

//    return 0;
//}
